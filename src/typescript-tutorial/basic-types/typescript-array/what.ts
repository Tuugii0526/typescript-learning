export const what=()=>{
    return `
    let names:string[]=[];
    names.push('hi how are you doing')

    let passwords:(string|number)[]=[];
     
    If you have noticed , passwords array's type is of mixed types.
    
    `
}