export const what=()=>{
    return `
    Typescript has two category types namely PRIMITIVE AND OBJECT TYPE.
    In PRIMITIVE TYPE, there includes:
        * string
        * number
        * boolean
        * null
        * undefined
        * symbol
    `
}