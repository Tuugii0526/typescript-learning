export const what=()=>{
    return `
    The optional and default parameters are confusing. When using optional parameters,  you check if to the optional parameter is initialised .


    In default parameter, you can omit or pass an argument as undefined . THen you can use default initialised value.
    `
}