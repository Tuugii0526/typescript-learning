export const what=()=>{
    return `
    When importing or exporting type , you can use either import type or export type .
     
    When working with interface , the type don't need to be included .
    
    
    `
}